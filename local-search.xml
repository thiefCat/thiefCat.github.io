<?xml version="1.0" encoding="utf-8"?>
<search>
  
  
  
  <entry>
    <title>CSE276A - Euler and Quaternion</title>
    <link href="/2025/11/08/CSE276A/CSE276A-pose/"/>
    <url>/2025/11/08/CSE276A/CSE276A-pose/</url>
    
    <content type="html"><![CDATA[<p>If we have constructed two coordinates (e.g., robot base link frameand camera frame), how to describe the transformation between the twocoordinates?</p><h3 id="transformation">Transformation</h3><p>The offset of the child frame’s origin (camera) from the parentframe’s origin (robot).</p><h3 id="rotation">Rotation</h3><p>A useful online rotation representation converter: <ahref="https://www.andre-gaschler.com/rotationconverter/">rotationconverter</a>.</p><h4 id="euler-angles">Euler Angles</h4><p>Any 3D orientation can be achieved by performing three sequentialrotations around a set of defined axes. The order matters for Eulerangles. The most common convention in robotics is Yaw (Z-axis), Pitch(Y-axis), and Roll (X-axis). As a result, a 3-dimensional euler vectorcannot describe an orientation unless we specify the rotation order.</p><h4 id="rotation-matrix">Rotation Matrix</h4><p>A 3x3 matrix where the columns are the child frame’s (camera’s) X, Y,and Z axes, as described in the parent frame’s (robot’s) coordinates.<span class="math display">$$R = \begin{bmatrix}| &amp; | &amp; | \\\vec{X}_C &amp; \vec{Y}_C &amp; \vec{Z}_C \\| &amp; | &amp; |\end{bmatrix}$$</span></p><h4 id="quaternion">Quaternion</h4><p>Instead of 3 separate rotations, a quaternion represents orientationas a single rotation around one specific axis in 3D space. Thequaternion representation is 4 dimensional.</p><h3 id="transformation-1">Transformation</h3><p>After obtaining transformation and rotation, this 4x4 transformationmatrix <span class="math inline"><em>H</em></span> is constructed byplugging in the 3x3 rotation matrix <spanclass="math inline"><em>R</em></span> and the 3x1 translation vector<span class="math inline"><em>T</em></span>.</p><p><span class="math display">$$H =\begin{bmatrix}&amp; &amp; &amp; \\&amp; R &amp; &amp; \vec{T} \\&amp; (3 \times 3) &amp; &amp; (3 \times 1) \\&amp; &amp; &amp; \\0 &amp; 0 &amp; 0 &amp; 1\end{bmatrix}$$</span></p><p>This matrix <span class="math inline"><em>H</em></span> can transformany point <spanclass="math inline"><em>P</em><sub><em>C</em></sub></span> defined inthe camera_frame coordinates directly into its corresponding point <spanclass="math inline"><em>P</em><sub><em>B</em></sub></span> in thebase_link frame coordinates with a single matrix multiplication:</p><p><span class="math display">$$\begin{bmatrix} P_{B,x} \\ P_{B,y} \\ P_{B,z} \\ 1 \end{bmatrix}=H\cdot\begin{bmatrix} P_{C,x} \\ P_{C,y} \\ P_{C,z} \\ 1 \end{bmatrix}$$</span></p>]]></content>
    
    
    <categories>
      
      <category>CSE276A</category>
      
    </categories>
    
    
    <tags>
      
      <tag>ROS2</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>CSE276A - ROS2 Commands</title>
    <link href="/2025/10/26/CSE276A/CSE276A-ROS2-Commands/"/>
    <url>/2025/10/26/CSE276A/CSE276A-ROS2-Commands/</url>
    
    <content type="html"><![CDATA[<h3 id="set-up">Set Up</h3><ul><li><p><code>ros2 pkg create &lt;package_name&gt; --build-type &lt;type&gt; --dependencies &lt;deps&gt;</code>:Automatically generates the entire file and directory structure for anew, empty ROS 2 package.</p></li><li><p><code>ros2 pkg list</code>: Shows all packages that the ROS 2environment can currently find.</p></li><li><p><code>ros2 pkg executables &lt;package_name&gt;</code>: Lists allthe nodes that a specific package provides.</p></li><li><p><code>ros2 pkg dependencies &lt;package_name&gt;</code>:</p></li><li><p><code>colcon build</code>: Run this command from the workspace,it will find all the ROS 2 packages in the src directory, compile themin the correct order based on their dependencies, and place theresulting files in the install and build directories. After build, weneed to source using <code>source install/setup.bash</code>.</p></li></ul><h3 id="launch-node">Launch Node</h3><ul><li><p><code>ros2 run &lt;package_name&gt; &lt;executable_name&gt;</code>:Launch a single node from a specific package.</p></li><li><p><code>ros2 launch &lt;package_name&gt; &lt;launch_file_name&gt;</code>:This launch file describes all the nodes that should be started, anyparameters they should be set with, and how they might be connected.Running the launch file enable us to launch a complexapplication.</p></li></ul><h3 id="node">Node</h3><ul><li><code>ros2 node list</code>: Shows all nodes currently active in thesystem.</li><li><code>ros2 node info &lt;node_name&gt;</code>: Lists all thepublishers, subscribers, services, and actions associated with thatspecific node.</li></ul><h3 id="topic">Topic</h3><ul><li><code>ros2 topic list</code>: Shows all topics currently active inthe system.</li><li><code>ros2 topic echo &lt;topic_name&gt;</code>: Prints the databeing published on a topic to the terminal in real-time.</li><li><code>ros2 topic pub &lt;topic_name&gt; &lt;message_type&gt; '&lt;data&gt;'</code>:Publishes a single message to a topic from the command line. Used fordebugging.</li><li><code>ros2 topic hz &lt;topic_name&gt;</code>: Reports the averagefrequency in hertz at which messages are being published on atopic.</li></ul><h3 id="service">service</h3><ul><li><code>ros2 service list</code>: Shows all services currently activein the system.</li><li><code>ros2 service type &lt;service_name&gt;</code>: Shows the datatype (interface) of the service.</li></ul>]]></content>
    
    
    <categories>
      
      <category>CSE276A</category>
      
    </categories>
    
    
    <tags>
      
      <tag>ROS2</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>CSE276A - ROS2</title>
    <link href="/2025/10/24/CSE276A/CSE276A-ROS2/"/>
    <url>/2025/10/24/CSE276A/CSE276A-ROS2/</url>
    
    <content type="html"><![CDATA[<h2 id="introduction">Introduction</h2><p>ROS 2 (Robot Operating System 2) is a framework that runs on anexisting OS. In a robot, there are separate programs that read cameradata, controlling wheels, planning paths .etc. ROS 2 provides a standardway for all these independent programs to discover each other andexchange information.</p><h3 id="nodes">Nodes</h3><p>A node is a single program responsible for one specific task.</p><div class="code-wrapper"><pre><code class="hljs python"><span class="hljs-keyword">import</span> rclpy <span class="hljs-comment"># ROS Client Library for Python</span><span class="hljs-keyword">from</span> rclpy.node <span class="hljs-keyword">import</span> Node<span class="hljs-keyword">class</span> <span class="hljs-title class_">SensorNode</span>(<span class="hljs-title class_ inherited__">Node</span>):    <span class="hljs-keyword">def</span> <span class="hljs-title function_">__init__</span>(<span class="hljs-params">self</span>):        <span class="hljs-comment"># Initialize the node with the name &#x27;sensor_node&#x27;</span>        <span class="hljs-built_in">super</span>().__init__(<span class="hljs-string">&#x27;sensor_node&#x27;</span>)        <span class="hljs-variable language_">self</span>.get_logger().info(<span class="hljs-string">&#x27;LIDAR sensor node has started.&#x27;</span>)</code></pre></div><h3 id="topics">Topics</h3><p>Topics are channels where nodes can send (publish) or receive(subscribe) data. This communication is asynchronous. E.g., the<code>sensor_node</code> needs to continuously broadcast sensor data toanyone who cares.</p><ul><li><p>Publisher</p><ul><li><code>self.publisher_ = self.create_publisher(String, 'chatter', 10)</code>:<code>String</code> specifies the message type, <code>'chatter'</code>is topic name, <code>10</code> is the the depth of the messagequeue.</li><li><code>self.timer = self.create_timer()</code>: register a timerevent, the event is scheduled by <code>rclpy.init()</code>.</li><li><code>rclpy.init()</code>: Initializes the rclpy library, firststep, mandatory.</li><li><code>rclpy.spin(node)</code>: ROS2 nodes are event-driven, thisfunction will keep the node running and constantly listen for events.The program will stay at this line until <code>Ctrl+C</code> isentered.</li><li><code>node.destroy_node()</code>: Gracefully cleans up the node’sinternal resources.</li><li><code>rclpy.shutdown()</code>: Shuts down the rclpy Library.</li></ul><p><div class="code-wrapper"><pre><code class="hljs Python"><span class="hljs-keyword">import</span> rclpy<span class="hljs-keyword">from</span> rclpy.node <span class="hljs-keyword">import</span> Node<span class="hljs-keyword">from</span> std_msgs.msg <span class="hljs-keyword">import</span> String<span class="hljs-keyword">class</span> <span class="hljs-title class_">Talker</span>(<span class="hljs-title class_ inherited__">Node</span>):    <span class="hljs-keyword">def</span> <span class="hljs-title function_">__init__</span>(<span class="hljs-params">self</span>):        <span class="hljs-built_in">super</span>().__init__(<span class="hljs-string">&#x27;talker&#x27;</span>)        <span class="hljs-variable language_">self</span>.publisher_ = <span class="hljs-variable language_">self</span>.create_publisher(String, <span class="hljs-string">&#x27;chatter&#x27;</span>, <span class="hljs-number">10</span>)        timer_period = <span class="hljs-number">0.5</span>        <span class="hljs-variable language_">self</span>.timer = <span class="hljs-variable language_">self</span>.create_timer(timer_period, <span class="hljs-variable language_">self</span>.timer_callback) <span class="hljs-comment"># timer_callback is called every 0.5 seconds</span>        <span class="hljs-variable language_">self</span>.count = <span class="hljs-number">0</span>    <span class="hljs-keyword">def</span> <span class="hljs-title function_">timer_callback</span>(<span class="hljs-params">self</span>):        msg = String()        msg.data = <span class="hljs-string">f&#x27;Hello ROS2: <span class="hljs-subst">&#123;self.count&#125;</span>&#x27;</span>        <span class="hljs-variable language_">self</span>.publisher_.publish(msg)        <span class="hljs-variable language_">self</span>.get_logger().info(<span class="hljs-string">f&#x27;Publishing: &quot;<span class="hljs-subst">&#123;msg.data&#125;</span>&quot;&#x27;</span>)        <span class="hljs-variable language_">self</span>.count += <span class="hljs-number">1</span><span class="hljs-keyword">def</span> <span class="hljs-title function_">main</span>(<span class="hljs-params">args=<span class="hljs-literal">None</span></span>):    rclpy.init(args=args)    node = Talker() <span class="hljs-comment"># Creates an instance of the Talker node class</span>    rclpy.spin(node)     node.destroy_node()    rclpy.shutdown()<span class="hljs-keyword">if</span> __name__ == <span class="hljs-string">&#x27;__main__&#x27;</span>:    main()</code></pre></div></p></li><li><p>Subscriber <div class="code-wrapper"><pre><code class="hljs python"><span class="hljs-keyword">import</span> rclpy<span class="hljs-keyword">from</span> rclpy.node <span class="hljs-keyword">import</span> Node<span class="hljs-keyword">from</span> std_msgs.msg <span class="hljs-keyword">import</span> String<span class="hljs-keyword">class</span> <span class="hljs-title class_">Listener</span>(<span class="hljs-title class_ inherited__">Node</span>):    <span class="hljs-keyword">def</span> <span class="hljs-title function_">__init__</span>(<span class="hljs-params">self</span>):        <span class="hljs-built_in">super</span>().__init__(<span class="hljs-string">&#x27;listener&#x27;</span>)        <span class="hljs-variable language_">self</span>.subscription = <span class="hljs-variable language_">self</span>.create_subscription(            String,          <span class="hljs-comment"># message type</span>            <span class="hljs-string">&#x27;chatter&#x27;</span>,       <span class="hljs-comment"># topic name</span>            <span class="hljs-variable language_">self</span>.listener_callback,  <span class="hljs-comment"># callback function</span>            <span class="hljs-number">10</span>               <span class="hljs-comment"># queue size</span>        )        <span class="hljs-variable language_">self</span>.subscription    <span class="hljs-keyword">def</span> <span class="hljs-title function_">listener_callback</span>(<span class="hljs-params">self, msg</span>):        <span class="hljs-variable language_">self</span>.get_logger().info(<span class="hljs-string">f&#x27;I heard: &quot;<span class="hljs-subst">&#123;msg.data&#125;</span>&quot;&#x27;</span>)<span class="hljs-keyword">def</span> <span class="hljs-title function_">main</span>(<span class="hljs-params">args=<span class="hljs-literal">None</span></span>):    rclpy.init(args=args)    node = Listener()    rclpy.spin(node)    node.destroy_node()    rclpy.shutdown()<span class="hljs-keyword">if</span> __name__ == <span class="hljs-string">&#x27;__main__&#x27;</span>:    main()</code></pre></div></p></li></ul><h3 id="services">Services</h3><p>Services are used for two-way communication. This communication issynchronous.</p><ul><li><p>Service Server</p><p><div class="code-wrapper"><pre><code class="hljs python"><span class="hljs-keyword">import</span> rclpy<span class="hljs-keyword">from</span> rclpy.node <span class="hljs-keyword">import</span> Node<span class="hljs-keyword">from</span> example_interfaces.srv <span class="hljs-keyword">import</span> AddTwoInts<span class="hljs-keyword">class</span> <span class="hljs-title class_">AddTwoIntsServer</span>(<span class="hljs-title class_ inherited__">Node</span>):    <span class="hljs-keyword">def</span> <span class="hljs-title function_">__init__</span>(<span class="hljs-params">self</span>):        <span class="hljs-built_in">super</span>().__init__(<span class="hljs-string">&#x27;add_two_ints_server&#x27;</span>)        <span class="hljs-comment"># create a service</span>        <span class="hljs-variable language_">self</span>.srv = <span class="hljs-variable language_">self</span>.create_service(AddTwoInts, <span class="hljs-string">&#x27;add_two_ints&#x27;</span>, <span class="hljs-variable language_">self</span>.add_two_ints_callback)    <span class="hljs-keyword">def</span> <span class="hljs-title function_">add_two_ints_callback</span>(<span class="hljs-params">self, request, response</span>):        <span class="hljs-variable language_">self</span>.get_logger().info(<span class="hljs-string">f&#x27;Received request: a=<span class="hljs-subst">&#123;request.a&#125;</span>, b=<span class="hljs-subst">&#123;request.b&#125;</span>&#x27;</span>)        response.<span class="hljs-built_in">sum</span> = request.a + request.b        <span class="hljs-variable language_">self</span>.get_logger().info(<span class="hljs-string">f&#x27;Returning response: sum=<span class="hljs-subst">&#123;response.<span class="hljs-built_in">sum</span>&#125;</span>&#x27;</span>)        <span class="hljs-keyword">return</span> response<span class="hljs-keyword">def</span> <span class="hljs-title function_">main</span>(<span class="hljs-params">args=<span class="hljs-literal">None</span></span>):    rclpy.init(args=args)    node = AddTwoIntsServer()    rclpy.spin(node)  <span class="hljs-comment"># spin to waiting for requests</span>    node.destroy_node()    rclpy.shutdown()<span class="hljs-keyword">if</span> __name__ == <span class="hljs-string">&#x27;__main__&#x27;</span>:    main()</code></pre></div></p></li><li><p>Client</p></li></ul><div class="code-wrapper"><pre><code class="hljs python"><span class="hljs-keyword">import</span> rclpy<span class="hljs-keyword">from</span> rclpy.node <span class="hljs-keyword">import</span> Node<span class="hljs-keyword">from</span> example_interfaces.srv <span class="hljs-keyword">import</span> AddTwoInts<span class="hljs-keyword">class</span> <span class="hljs-title class_">AddTwoIntsClient</span>(<span class="hljs-title class_ inherited__">Node</span>):    <span class="hljs-keyword">def</span> <span class="hljs-title function_">__init__</span>(<span class="hljs-params">self</span>):        <span class="hljs-built_in">super</span>().__init__(<span class="hljs-string">&#x27;add_two_ints_client&#x27;</span>)        <span class="hljs-variable language_">self</span>.client = <span class="hljs-variable language_">self</span>.create_client(AddTwoInts, <span class="hljs-string">&#x27;add_two_ints&#x27;</span>)        <span class="hljs-keyword">while</span> <span class="hljs-keyword">not</span> <span class="hljs-variable language_">self</span>.client.wait_for_service(timeout_sec=<span class="hljs-number">1.0</span>):            <span class="hljs-variable language_">self</span>.get_logger().info(<span class="hljs-string">&#x27;Service not available, waiting...&#x27;</span>)        <span class="hljs-variable language_">self</span>.request = AddTwoInts.Request()        <span class="hljs-variable language_">self</span>.request.a = <span class="hljs-number">5</span>        <span class="hljs-variable language_">self</span>.request.b = <span class="hljs-number">7</span>    <span class="hljs-keyword">def</span> <span class="hljs-title function_">send_request</span>(<span class="hljs-params">self</span>):        <span class="hljs-variable language_">self</span>.future = <span class="hljs-variable language_">self</span>.client.call_async(<span class="hljs-variable language_">self</span>.request)        rclpy.spin_until_future_complete(<span class="hljs-variable language_">self</span>, <span class="hljs-variable language_">self</span>.future) <span class="hljs-comment"># wait for response</span>        <span class="hljs-keyword">return</span> <span class="hljs-variable language_">self</span>.future.result()<span class="hljs-keyword">def</span> <span class="hljs-title function_">main</span>(<span class="hljs-params">args=<span class="hljs-literal">None</span></span>):    rclpy.init(args=args)    node = AddTwoIntsClient()    response = node.send_request()    node.get_logger().info(<span class="hljs-string">f&#x27;Result of addition: <span class="hljs-subst">&#123;response.<span class="hljs-built_in">sum</span>&#125;</span>&#x27;</span>)    node.destroy_node()    rclpy.shutdown()<span class="hljs-keyword">if</span> __name__ == <span class="hljs-string">&#x27;__main__&#x27;</span>:    main()</code></pre></div>]]></content>
    
    
    <categories>
      
      <category>CSE276A</category>
      
    </categories>
    
    
    <tags>
      
      <tag>ROS2</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Shell - How Shell Works</title>
    <link href="/2024/06/24/CS_Basis/Shell2/"/>
    <url>/2024/06/24/CS_Basis/Shell2/</url>
    
    <content type="html"><![CDATA[<h3 id="configuration-file">Configuration file</h3><ul><li><code>~/.zshrc</code>: The primary config file for zsh</li><li><code>~/.bashrc</code>: The primary config file for bash (on linuxsystems) The shell automatically reads (it “sources”) this file everytime you open a new terminal. The source command is a built-in shellcommand that executes a script in the current shell session. Forexample, if the script exports a variable (e.g., export MY_VAR=“hello”),that variable becomes part of your current shell’s environment.</li></ul><h3 id="how-the-shell-executes-programs">How the shell executesprograms</h3><p>When a command is entered in the shell, it follows these steps toexecute:</p><ol type="1"><li><strong>Parsing:</strong> The shell reads the command line input andbreaks it into components. <strong>The first word is the command(program), and the following words are arguments.</strong></li><li><strong>Command Lookup:</strong> The shell searches for the commandin the directories listed in the PATH environment variable.<div class="code-wrapper"><pre><code class="hljs bash"><span class="hljs-built_in">echo</span> <span class="hljs-variable">$PATH</span></code></pre></div></li><li><strong>Forking:</strong> The shell creates a new process (childprocess) to execute the command.</li><li><strong>Return:</strong> After the child process finishes, controlreturns to the user.</li></ol><h3 id="executing-programs">Executing programs</h3><div class="code-wrapper"><pre><code class="hljs bash">% <span class="hljs-built_in">echo</span> <span class="hljs-variable">$PATH</span>/Users/.../bin:/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin</code></pre></div><p>The <code>echo $PATH</code> command executes the program<code>echo</code>. The shell searches for it in the directories definedin <code>$PATH</code>. To find where the <code>echo</code> programresides:</p><div class="code-wrapper"><pre><code class="hljs bash">% <span class="hljs-built_in">which</span> <span class="hljs-built_in">echo</span><span class="hljs-built_in">echo</span>: shell built-in <span class="hljs-built_in">command</span></code></pre></div><p>Other examples:</p><div class="code-wrapper"><pre><code class="hljs bash">% <span class="hljs-built_in">which</span> <span class="hljs-built_in">ls</span>/bin/ls% <span class="hljs-built_in">which</span> <span class="hljs-built_in">mkdir</span>/bin/mkdir% <span class="hljs-built_in">which</span> npm/Users/.../bin/npm% <span class="hljs-built_in">which</span> python/opt/anaconda3/bin/python</code></pre></div><p>Check where is my python: <div class="code-wrapper"><pre><code class="hljs shell"><span class="hljs-meta prompt_"># </span><span class="language-bash">There are two python3 installed on my commputer</span><span class="hljs-meta prompt_">% </span><span class="language-bash"><span class="hljs-built_in">which</span> -a python3</span>/opt/miniconda3/bin/python3/usr/bin/python3<span class="hljs-meta prompt_"></span><span class="hljs-meta prompt_"># </span><span class="language-bash">But this <span class="hljs-built_in">command</span> only <span class="hljs-built_in">return</span> one</span><span class="hljs-meta prompt_">% </span><span class="language-bash"><span class="hljs-built_in">which</span> python3</span>/opt/miniconda3/bin/python3</code></pre></div> <strong>When you havemultiple versions of the same command, The shell executes the first oneit finds, searching the directories in your <code>$PATH</code> from leftto right.</strong></p><h3 id="types-of-commands">Types of Commands</h3><ul><li><p>Built-in commands A built-in command is a command that is part ofthe shell program itself. Examples:<code>cd, echo, exit, export, alias, pwd</code>. <div class="code-wrapper"><pre><code class="hljs shell"><span class="hljs-meta prompt_">% </span><span class="language-bash"><span class="hljs-built_in">type</span> <span class="hljs-built_in">pwd</span></span>     pwd is a shell builtin</code></pre></div></p></li><li><p>External Commands An external command is a separate program thatexists as a file on your system. Examples:<code>ls, grep, git, python, node</code>.</p><p><div class="code-wrapper"><pre><code class="hljs shell"><span class="hljs-meta prompt_">% </span><span class="language-bash"><span class="hljs-built_in">type</span> <span class="hljs-built_in">ls</span></span>ls is /bin/ls</code></pre></div></p></li></ul><h2 id="advanced">Advanced</h2><h3 id="redirection">Redirection</h3><p>In the shell, each program has an input stream and an output stream —by default, the keyboard and terminal. We can redirect input or outputusing <code>&lt; file</code> or <code>&gt; file</code>.</p><div class="code-wrapper"><pre><code class="hljs bash">% <span class="hljs-built_in">echo</span> <span class="hljs-string">&quot;Hello, World&quot;</span> &gt; output.txt</code></pre></div><ul><li><code>echo "Hello, World"</code>: prints text to the terminal.</li><li><code>&gt;</code>: redirects output to the file specified on itsright.</li></ul><p>Result: the text “Hello, World” is written to<code>output.txt</code>. <code>&gt;</code> overwrites a file, while<code>&gt;&gt;</code> appends to it.</p><h3 id="pipes">Pipes</h3><p>Pipes (<code>|</code>) pass the output of one command as input toanother.</p><div class="code-wrapper"><pre><code class="hljs bash">% <span class="hljs-built_in">ls</span> -l | grep <span class="hljs-string">&quot;.txt&quot;</span>-rw-r--r--  1 user  staff  13 Jun 24 16:18 output.txt</code></pre></div><p>Explanation: - <code>ls -l</code>: lists files in long format. -<code>|</code>: passes output to the next command. -<code>grep ".txt"</code>: filters for <code>.txt</code> files.</p><p>Combining redirection and pipes:</p><div class="code-wrapper"><pre><code class="hljs bash">% <span class="hljs-built_in">ls</span> -l | grep <span class="hljs-string">&quot;.txt&quot;</span> &gt; txt_files_list.txt% <span class="hljs-built_in">cat</span> txt_files_list.txt-rw-r--r--  1 user  staff  13 Jun 24 16:18 output.txt-rw-r--r--  1 user  staff   0 Jun 24 16:34 txt_files_list.txt</code></pre></div><hr /><h2 id="useful">Useful</h2><h3 id="alias">Alias</h3><div class="code-wrapper"><pre><code class="hljs bash">bash-3.2$ <span class="hljs-built_in">alias</span> gs=<span class="hljs-string">&quot;git status&quot;</span>bash-3.2$ gsOn branch mainnothing to commit, working tree cleanbash-3.2$ git statusOn branch mainnothing to commit, working tree clean</code></pre></div><p>To make an alias persistent, include it in configuration files like<code>.bashrc</code> or <code>.zshrc</code>.</p>]]></content>
    
    
    <categories>
      
      <category>Computer Basis</category>
      
    </categories>
    
    
    <tags>
      
      <tag>shell</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>Shell - Basic Syntax</title>
    <link href="/2024/06/24/CS_Basis/Shell1/"/>
    <url>/2024/06/24/CS_Basis/Shell1/</url>
    
    <content type="html"><![CDATA[<h3 id="what-is-the-shell">What is the shell?</h3><p><strong>A shell is a program that takes your commands, interpretsthem, and tells the operating system what to do.</strong> It is acommand-line interface (CLI). The shell in macOS before Catalina is bash(Bourne Again Shell) and currently it’s Zsh (Z Shell). The shell allowsusers to execute commands, run programs, and manage system resources.While the GUI defines many use cases for interacting with the OS, totake full advantage of system tools, we must use the shell.</p><h3 id="basic-shell-commands">Basic shell commands</h3><div class="code-wrapper"><pre><code class="hljs shell">. # current directory.. # parent directorylsmkdircd # change directorypwd # print working directoryecho &quot;text&quot; # prints the specified text to the terminalcat # displays the contents of the specified filetouch example.txt # creates a filegrep &quot;Hello&quot; example.txt # searches for the string “Hello” in a file named example.txt and returns the line containing itexport #</code></pre></div><h3 id="basic-syntax">Basic Syntax</h3><p>Every shell script starts with a shebang line: <div class="code-wrapper"><pre><code class="hljs shell"><span class="hljs-meta prompt_">#</span><span class="language-bash">!/bin/bash</span></code></pre></div> Thistells the system to run the script with Bash.</p><ul><li><p>Define variables <div class="code-wrapper"><pre><code class="hljs shell">name=&quot;Alice&quot; # no space around &quot;=&quot;echo $nameecho &quot;Hello, $&#123;name&#125;!&quot;</code></pre></div> In this example, <code>name</code>is variable name, <code>$name</code> represents getting the value of thevariable.</p></li><li><p>Command substitution <div class="code-wrapper"><pre><code class="hljs shell"><span class="hljs-meta prompt_">% </span><span class="language-bash"><span class="hljs-built_in">date</span></span>Fri Oct 24 01:19:10 PDT 2025today=$(date) # $(date) runs the date command first, and replace it with its outputecho &quot;Today is $today&quot;</code></pre></div> <code>$()</code> means runninga command and store its output.</p></li><li><p>If statements <div class="code-wrapper"><pre><code class="hljs shell">if [ $age -gt 18 ]; then    echo &quot;Adult&quot;else    echo &quot;Minor&quot;fi</code></pre></div> Common operators:<code>-eq, -ne, -gt, -ge, -lt, -le</code>.</p></li><li><p>For loop <div class="code-wrapper"><pre><code class="hljs shell">for var in item1 item2 item3; do  echo &quot;$var&quot;done</code></pre></div></p></li></ul><div class="code-wrapper"><pre><code class="hljs shell">for f in *.txt; do # get all *.txt in the current directory  echo &quot;file: $f&quot;done</code></pre></div><ul><li><p>While loop <div class="code-wrapper"><pre><code class="hljs shell">count=1while [ $count -le 5 ]; do  echo &quot;Count $count&quot;  ((count++))done</code></pre></div></p></li><li><p>Arithmetic <code>(( ))</code> is used for arithmetic evaluation(like addition, subtraction, comparisons, and increments) inBash.</p></li></ul><div class="code-wrapper"><pre><code class="hljs shell">((a = 5 + 3))echo $a   # 8((a++))((c += 5))((d *= 2))</code></pre></div><div class="code-wrapper"><pre><code class="hljs shell">a=5b=3if ((a &gt; b)); then  echo &quot;a is greater&quot;fifor ((i=1; i&lt;=5; i++)); do  echo &quot;Number $i&quot;done</code></pre></div><ul><li><p>Functions <div class="code-wrapper"><pre><code class="hljs shell">greet() &#123;    echo &quot;Hello, $1&quot;&#125;greet &quot;Alice&quot;</code></pre></div> <code>$1</code> refers to the firstargument passed to the function.</p></li><li><p>Exit Status Every command in Linux returns an exit status. 0means success, and non-zero means failure. <code>$?</code> is a specialvariable that stores the exit code of the most recent command.</p></li></ul><div class="code-wrapper"><pre><code class="hljs shell">ls /etcecho $?   # 0 = successls /no/such/fileecho $?   # non-zero = error</code></pre></div><div class="code-wrapper"><pre><code class="hljs shell">if ls /no/such/file; then # if the ls command succeed, then print &quot;Command succeeded&quot;  echo &quot;Command succeeded&quot;else  echo &quot;Command failed&quot;fi</code></pre></div><h3 id="tests">Tests</h3><p>Tests are used in conditional statements. - <code>-f file</code>,File exists and is a regular file <div class="code-wrapper"><pre><code class="hljs shell">if [ -f &quot;config.txt&quot; ]; then  echo &quot;Config file found&quot;else  echo &quot;No config file&quot;fi</code></pre></div> - <code>-d file</code>,file is a directory - <code>-z "$str"</code>, string is empty -<code>-n "$str"</code>, string is not empty - <code>"$a" != "$b"</code>,strings are not equal</p><h3 id="special-parameters">Special Parameters</h3><ul><li><code>$0</code>, name of the current script</li><li><code>$1</code>, first parameter</li><li><code>$#</code>, number of parameters</li><li><code>$$</code>, current script’s process ID</li><li><code>$?</code>, exit status of the last command</li><li><code>$SHELL</code>, current shell path</li><li><code>$PATH</code>, directories to search for executables</li></ul>]]></content>
    
    
    <categories>
      
      <category>Computer Basis</category>
      
    </categories>
    
    
    <tags>
      
      <tag>shell</tag>
      
    </tags>
    
  </entry>
  
  
  
  <entry>
    <title>My first Blog</title>
    <link href="/2024/06/23/test/"/>
    <url>/2024/06/23/test/</url>
    
    <content type="html"><![CDATA[<h2 id="post">Post</h2><h3 id="create-a-new-post">Create a new post</h3><div class="code-wrapper"><pre><code class="hljs bash">$ hexo new <span class="hljs-string">&quot;Name&quot;</span>$ hexo new post <span class="hljs-string">&quot;topic&quot;</span> --path <span class="hljs-string">&quot;folder/topic&quot;</span></code></pre></div><h3 id="include-image-in-a-post">Include image in a post</h3><p>Move the image to the folder created automatically by Hexo, and thenuse the relative path to refer to the image.</p><p>Using hexo syntax, but unable to resize the image: <div class="code-wrapper"><pre><code class="hljs Markdown">&#123;% asset<span class="hljs-emphasis">_img sample.jpeg &#x27;This is my picture&#x27; %&#125;</span></code></pre></div></p><img src="/2024/06/23/test/sample.jpeg" class="" title="This is my picture"><p>Or directly use an <code>&lt;img&gt;</code> tag:</p><div class="code-wrapper"><pre><code class="hljs html"><span class="hljs-tag">&lt;<span class="hljs-name">img</span> <span class="hljs-attr">src</span>=<span class="hljs-string">&quot;sample.jpeg&quot;</span> <span class="hljs-attr">style</span>=<span class="hljs-string">&quot;width: 300px; height: auto;&quot;</span> <span class="hljs-attr">alt</span>=<span class="hljs-string">&quot;Sample Image&quot;</span>&gt;</span></code></pre></div><p><img src="sample.jpeg" style="width: 300px; height: auto;" alt="Sample Image"></p><p>With centered alignment:</p><div class="code-wrapper"><pre><code class="hljs html"><span class="hljs-tag">&lt;<span class="hljs-name">div</span> <span class="hljs-attr">style</span>=<span class="hljs-string">&quot;text-align: center;&quot;</span>&gt;</span>  <span class="hljs-tag">&lt;<span class="hljs-name">img</span> <span class="hljs-attr">src</span>=<span class="hljs-string">&quot;sample.jpeg&quot;</span> <span class="hljs-attr">style</span>=<span class="hljs-string">&quot;width: 300px; height: auto;&quot;</span> <span class="hljs-attr">alt</span>=<span class="hljs-string">&quot;Sample Image&quot;</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-name">div</span>&gt;</span></code></pre></div><div style="text-align: center;"><p><img src="sample.jpeg" style="width: 300px; height: auto;" alt="Sample Image"></p></div><hr /><h2 id="blog">Blog</h2><h3 id="generate-static-files">Generate static files</h3><div class="code-wrapper"><pre><code class="hljs bash">$ hexo g</code></pre></div><h3 id="run-server">Run server</h3><div class="code-wrapper"><pre><code class="hljs bash">$ hexo s</code></pre></div><h3 id="deploy">Deploy</h3><div class="code-wrapper"><pre><code class="hljs bash">$ hexo d</code></pre></div><h3 id="generate-and-deploy">Generate and Deploy</h3><div class="code-wrapper"><pre><code class="hljs bash">$ hexo g -d</code></pre></div><h2 id="create-a-new-page">Create a new page</h2><div class="code-wrapper"><pre><code class="hljs bash">$ hexo new page <span class="hljs-string">&quot;name&quot;</span></code></pre></div><h3 id="creating-a-new-navigation-bar">Creating a new navigationbar</h3><ol type="1"><li>Create a new <code>.ejs</code> file in<code>/themes/fluid/layout/</code></li><li>Add page attributes in <code>_config.fluid.yml</code>, and include anew menu item inside <code>navbar.menu</code></li><li>Modify <code>/themes/fluid/languages/en.yml</code> to add titles andsubtitles</li></ol>]]></content>
    
    
    <categories>
      
      <category>test</category>
      
    </categories>
    
    
    <tags>
      
      <tag>test</tag>
      
    </tags>
    
  </entry>
  
  
  
  
</search>
